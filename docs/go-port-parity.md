# Go port — §3 "Verified contract" parity checklist

Walked at M9 against the Go implementation. `✅` = behaviour matches the bash
source; `➕` = deliberate, documented divergence (a fix or an unavoidable
transport change); nothing is an accidental regression.

| # | §3 item | verdict | notes |
|---|---|---|---|
| 1 | Run submit + poll | ✅ (➕ transport text) | `internal/api.Ask`: `POST {base}/v1/runs` then poll `GET .../v1/runs/{id}`; `Authorization: Bearer`, `Content-Type: application/json`, `Idempotency-Key: hh-<epoch>-<RANDOM><RANDOM>` minted once, `X-Hermes-Session-Id` / `-Key` iff the record has them (kept on the post-reject retry). Per-request connect/total timeouts via `NewHTTPClient`. POST non-2xx → fixed `sleep 2`, up to `HERMES_API_RETRIES` (4 attempts); 2xx w/o `.run_id` → fail. Poll interval `HERMES_API_POLL_INTERVAL`; terminal `completed` (non-empty `.output`) / `failed`\|`cancelled` (`.output // .error // "no detail"`, trunc 400) / non-terminal keep polling / unknown → log + poll; whole loop bounded by `HERMES_API_RUN_TIMEOUT`; poll HTTP errors logged, not fatal. `.usage` not read. **➕** the "unreachable / cannot reach" strings say the Go HTTP error instead of `curl <rc> <err>` — no curl to quote; structure (`… - <err>, last HTTP <code>`) preserved. |
| 2 | Base URL | ✅ | `Client.base()` = `strings.TrimSuffix(BaseURL, "/")` (one slash) + `"/p/"+Profile` when set. Endpoints append their own `/v1/…` / `/api/…`. |
| 3 | Session-id drop / recap | ✅ | On `400\|404\|422` with a body `session_id` not yet dropped: warn `server rejected session_id (HTTP <c>) - retrying without it, local recap on`, set `dropSess`, `continue` (no attempt++, no pause); `X-Hermes-Session-*` headers stay. `Threaded = !dropSess`. In `loop`: `round>1 && !Threaded → recap` (latched, never reset). `401\|403` here fatal immediately. Persistent `400\|404\|422` after the drop → generic fail. |
| 4 | Envelope + extraction ladder | ✅ | `loop.extractObj`: strip `` ```json `` / `` ``` `` prefix and `` ``` `` suffix (literal), accept if the trimmed remainder is a JSON **object**, else first `{` … last `}` byte slice of the **original** reply (`end > start`). Then: valid object → `calls = len(.calls // [])`, `final = .final` iff a JSON string; else botched-but-structured (`(?i)"(calls\|tool)"[[:space:]]*:`) with `fixups < 2` → resend the exact `{"error":"…"}` nudge + turnlog line + `round++`; else prose → strip leading `[INPUT_REQUIRED]`, ltrim the first line only. |
| 5 | `args` as a JSON string | ✅ | `loop.callArgs`: `.args // .arguments // {}`; if the value is a JSON string, unwrap once (parse its content, `{}` on failure). `tool = .tool // .name // ""`. |
| 6 | Tool results back | ✅ | `send = {"results":[ {tool, args, exit_code, output:Scrub(out)} (+ context:Scrub(ctx) iff non-empty) ]}` — key order + `exit_code` name preserved, `args` echoed, HTML escaping off so `<>&` survive like jq. Recap prefix `"[conversation so far this turn]\n<turnlog>\n[latest tool results]\n<send>"` when `recap`. `turnlog` line: `"[round N]   <tool>(<compact args, ≤120 B>) -> exit <code>"`. |
| 7 | Rounds cap | ✅ | `MaxRounds` (default 8), `round` from 1; `round > MaxRounds` checked after the empty-envelope branch, before executing calls; fixup resends also `round++`. Net: 8 executed rounds, the 9th reply-with-calls → `BLOCKED: Hermes still requesting data after 8 rounds.` |
| 8 | ID schemes | ✅ | `session.New`: shared `ts = %Y%m%dT%H%M%S` UTC; local id `hh_<ts>_<sha1(cwd+RANDOM)[:6]>`, `hermes_session_id` `hh-<sha1(cwd)[:8]>-<ts>`, `hermes_session_key` `hermes-hands:<sha1(cwd)[:16]>`, by-cwd pointer `<dir>/by-cwd/<sha1(cwd)>.id` (40 hex). `sha1` = `crypto/sha1`, input without a trailing newline. `BumpTurn` adopts the server `.session_id` when non-empty and different. |
| 9 | Config / secrets | ✅ (➕ narrowed parser, ➕ knob source) | `config.Load`: resolve `cfg`/`sec` paths from the process env; apply `cfg` (overrides env for the live-read keys), then `sec` only while `HERMES_API_URL`\|`HERMES_API_KEY` empty. `setup` writes `config` (3 comment lines) + `secrets` (`export …=<printf %q>`), `chmod 600` secrets / `700` dir, offers the `~/.bashrc` source line. **➕** the file parser accepts only `KEY=value` / `export KEY=value` with `"…"` / `'…'` / `$'…'` / bare-`printf %q` quoting (any other line ignored — bash `source`d arbitrary shell; locked decision #4). **➕** the API tuning knobs + state dir are read from the process env only — bash cached them at lib-source time *before* `hh_load_config`, so its config file never influenced them either; `config.example` ships every key commented so real behaviour is identical. |
| 10 | Tools + approval | ✅ (➕ `edit_file` literal, ➕ `read_file` exit, ➕ parent-dir) | `dispatch.Dispatch` with the alias sets and jq `//` arg fallbacks. `shell`: denylist → exit 126; approval `Deny`→125 `(declined by operator)` / `AbortTurn`→`ErrAbortTurn`; `.timeout` iff `^[0-9]+$` else default 120; on non-zero exit, `context` = `cwd:` + `git status -s` (capped in-shell at 1200) + `\n--- make targets ---\n` + parsed targets iff the command contained `" make "` (15 s each). `read_file`: repo-jailed, output capped at `HERMES_HANDS_MAX_OUTPUT`. `write_file`/`edit_file`: `diff -u` (labels `a/<p>` `b/<p>`, 4000-byte cap) + approval; success `wrote <p> (<bytes> bytes)` / `edited <p>`. Missing arg → 2, jail refusal → 1, unknown tool → 2 (`have: shell, read_file, write_file, edit_file`). **➕** `edit_file` is a literal exactly-once `strings.Replace` (`strings.Count == 1`), dropping the bash `awk sub()` regex + `&` behaviour (plan §2 #18 / decision #3); also allows a multi-line `old`. **➕** `read_file` errors return exit 0 with a `cat: …`-style message (bash's `_hh_cap` masks the real exit for files under the pipe buffer, and **crashes the process with exit 141 on files above it** — Go just caps). **➕** `write_file`/`edit_file` to a path whose parent dir doesn't exist → refused with bash's `outside repo root` message (bash `_hh_resolve_in_repo` needs `cd $(dirname)`); the `mkdir -p` is vestigial in both. |
| 11 | Denylist (verbatim) + repo jail | ✅ | `cmdBlocked`: `" ssh "` `" scp "` `" sftp "` `" rsync "` → `ssh/scp/rsync to other hosts`; `" sudo "` `" doas "` → `privilege escalation`; `rm -rf /` `rm -fr /` `:(){ :\|:& };:` → `destructive`; `" curl "`/`" wget "` + (`\| sh` `\|sh` `\| bash` `\|bash`) → `pipe-to-shell download`; then each non-empty `HERMES_HANDS_DENY` glob (real shell-glob matcher: `*` crosses `/`, `?`, `[set]`/`[!set]`) against the raw command → `matches HERMES_HANDS_DENY (<g>)`. `resolveInRepo`: parent resolved physically (`EvalSymlinks` + `IsDir`), literal basename re-appended, must equal the repo root or start with `root + "/"`. `shell` is not jailed. |
| 12 | Secret scrub (verbatim) | ✅ | `redact.Scrub`: the 7 `sed -E` rules as RE2, `(?i)` on 1–3, applied in source order; `\1` → `${1}`; `«redacted»` / `«redacted-aws-key»` / `«redacted-private-key»` byte-exact (U+00AB / U+00BB). |
| 13 | TLS | ✅ | `config.RequireHTTPS`: `https://` ok; `http://127.0.0.1*` / `localhost*` / `0.0.0.0*` / `[::1]*` ok iff `HERMES_HANDS_ALLOW_HTTP=="1"` (with the warning), else fatal; other `http://` fatal `<name> must be https:// - …`; non-http(s) fatal `<name> is not a http(s) URL: <url>`. |
| 14 | CLI surface | ✅ (➕ `--session` msg, ➕ Ctrl-C) | `parseArgs` first-match table: `-h`/`--help`, `-v`/`--version`/`version`, `check`/`--check`, `sessions`/`--list`, `setup`, `-c`/`--continue`, `--new`, `--session <id>`, `--yolo` (→ `HERMES_HANDS_APPROVE=auto` before load), `-` (stdin), `--` (rest → oneshot), other `-*` → `BLOCKED: unknown option: <tok>` exit 1, bare word → oneshot. Dispatch: stdin → `runTurn(strip trailing \n)`; oneshot non-empty → `runTurn`; else REPL (preflight: `Check` silent → secrets readable ? `API preflight failed - run: hermes-hands check` : `not configured yet - run: hermes-hands setup`). REPL: `""`/`" "` ignored; `/exit`\|`/quit`\|`/q`; `/help`\|`/h`\|`/?`; `/check`; `/new` → `— new session <id> —`; `/sessions`; else `ui_rule`/`ui_you`/`ui_working`/loop/`ui_answer` (all to stderr); Ctrl-D → newline + break. Banner + prompt (`you ❯ `) byte-exact. **➕** `--session` with no id → `hermes-hands: --session needs an id` exit 1 (bash's raw `${1:?…}` bash-ism). **➕** Ctrl-C during a turn cancels the turn context + kills the shell child and returns to the prompt (`— interrupted —`); bash has no SIGINT trap and just dies. **➕** `/check` prints the result and stays in the REPL; bash's `hh_die` inside `/check` would `exit` the whole REPL. |
| 15 | Env / config keys (complete) | ✅ | All keys + defaults present in `config.Config` (`HERMES_API_URL/KEY/PROFILE`, `HERMES_API_{CONNECT_TIMEOUT 5, MAX_TIME 30, POLL_INTERVAL 2, RETRIES 3, RUN_TIMEOUT 600}`, `HERMES_HANDS_{APPROVE ask, DENY, MAX_ROUNDS 8, RUN_TIMEOUT 120, MAX_OUTPUT 20000, ALLOW_HTTP, VERBOSE, STATE, CONFIG, SECRETS, INSTRUCTIONS}`, `NO_COLOR`/`TERM`/`COLUMNS` in `ui`). `HERMES_HANDS_STATE` when set is the state dir literally. Instructions ladder: `$HERMES_HANDS_INSTRUCTIONS` \| `~/.config/hermes-hands/instructions.md` (first readable & non-empty) else the embedded `share/instructions.md`. **➕** the embedded builtin has its trailing newline trimmed to match the override-file path (bash trimmed `$(cat file)` but not the bundled base64 var). |
| 16 | `check` | ✅ | `Client.Check`: `GET {base}/v1/capabilities` (+ `Accept: application/json`); success = curl-ok && 2xx && JSON → `API OK: <.model // .runtime.mode // "hermes"> @ <base>`; `401\|403` fatal `HERMES_API_KEY rejected`; `404` fatal `wrong base URL / profile, or API server disabled`; else `sleep 2`, retry to `HERMES_API_RETRIES`, then fatal `cannot reach <base>/v1 - <err>, last HTTP <code>`. |
| 17 | Title mirroring | ✅ | After the first successful turn only (`SetTitleLocal` when `.title` empty): trim to 72 bytes with `\n`→space, write `.title`; then best-effort `Client.SetTitle` → `PATCH {TrimSuffix(base,"/v1")}/api/sessions/<hermes_session_id>` `{"title":<t>}`, 8 s, every failure swallowed. |

## Build / test matrix (M9)

- `gofmt -l` (non-vendor): clean
- `go vet ./...`: pass
- `go test ./...`: 10 packages ok, 85 tests, 0 fail, 0 skip (with `bash` present)
- `CGO_ENABLED=0 go build ./...` for `{linux,darwin,windows}×{amd64,arm64}`: all build
- `go mod verify`: all modules verified; deps vendored under `vendor/`
- `VERSION` unchanged at `0.1.0` (never shipped)

## Deferred

- `release.yml` (tag `v*` → GOOS/GOARCH matrix upload) — a follow-up PR; the
  local `make release` target is in place.
- Live test against the real Hermes gateway — blocked by the WSL network policy;
  the wire contract is preserved so it is apples-to-apples when run.
