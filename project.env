# hermes-hands project coordinates (public, committed). Single source of truth
# for the GitHub slug used by install.sh (release URL / source clone) and
# build.sh (bundle header). Override at install time with $HERMES_HANDS_REPO.
REPO_SLUG=Zesales/hermes-hands
REPO_URL=https://github.com/Zesales/hermes-hands
